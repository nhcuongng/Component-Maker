import{mkdirSync,writeFileSync}from"fs";import ora from"ora";import{createIndexChildContent,createScssContent}from"../createFileContent/index.mjs";export const createChildFolder=(e,t)=>{const r=ora("Writing folder structure...").start();r.start(),mkdirSync(e,{recursive:!0},(e=>{if(e)throw e})),writeFileSync(`${e}/index.tsx`,createIndexChildContent(t)),writeFileSync(`${e}/${t}.module.scss`,createScssContent(t)),r.succeed(`${t} created successfully!`)};